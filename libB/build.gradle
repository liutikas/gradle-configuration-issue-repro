import com.example.build.BundleInsideHelper

plugins {
    id("com.android.library")
    id("com.github.johnrengelman.shadow")
}

android {
    compileSdkVersion(30)
}

BundleInsideHelper.forInsideAar(
        project,
        /* from = */ "com.example.libA",
        /* to =   */ "com.example.libB.external"
)

dependencies {
    implementation(project(":libA"))
}
